package com.example.android_task_rasool.model

data class CategoryModel(
    val categoryTitle: String,
    val bgColor: Int,
    val bgCircle: Int // Use this flag to determine which layout to use
)
